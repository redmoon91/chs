#if(${dotThemeContainers})
   #set($containerTheme = ${dotThemeContainers})
#else
   #set($containerTheme = "/application/themes/travel/")
#end

#set($containerPath = ${containerTheme} + "containers/calendarevent.vtl")
#dotParse($containerPath)